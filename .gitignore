# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# wasm build artefacts
build
src/assets/demo/
src/assets/transformers/
src/assets/wasm/

# python venvs (CMake transformers/.venv for ONNX, wasm/semantic/.venv for the
# production model GGUF converters — see `make models` / `make onnx-models`)
**/.venv/

# bge-embed: large GGUF weights (fetched via `make embed-model`) + the
# downloaded UnicodeData.txt cache used to regenerate unicode-data.c.
src/assets/models/
wasm/bge-embed/tools/unicode-data.txt

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ai
CLAUDE.md
.claude

# deploy
keys
secrets

# Chrome --user-data-dir for CDP debugging (launched under the repo root)
chrome-debug-profile/

